<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8" />
        <title>{% block title %}Welcome!{% endblock %}</title>
        {% stylesheets '@bootstrap_css' %}
        <link rel="stylesheet" type="text/css" media="screen" href="{{ asset_url }}"/>
        {% endstylesheets %}
        {% block stylesheets %}{% endblock %}
        <link rel="icon" type="image/x-icon" href="{{ asset('favicon.ico') }}" />
        <link rel="stylesheet" type="text/css" media="screen" href="{{ asset('css/bootstrap.min.css') }}"/>
        <link rel="stylesheet" type="text/css" media="screen" href="{{ asset('css/style.css') }}"/>
    </head>
    <body>
        <div id="wrapper">
        <div class="header">
            <nav class="navbar navbar-site navbar-default">
                <div class="container">
                    <div class="navbar-header">
                        <button data-target=".navbar-collapse" data-toggle="collapse" class="navbar-toggle" type="button"> <span class="sr-only">Toggle navigation</span> <span class="icon-bar"></span> <span class="icon-bar"></span> <span class="icon-bar"></span> </button>
                        <a href="{{ path('homepage')}}" class="navbar-brand logo logo-title">

                            <span class="logo-icon"><i class="icon icon-home ln-shadow-logo shape-0"></i> </span> BORDEAUX<span>HOME </span> </a> </div>
                    <div class="collapse navbar-collapse navbar-ex-collapse">
                        {{ knp_menu_render('user', {'currentClass': 'active', 'template': 'AppBundle:menu:knp_menu.html.twig'}) }}
                        {{ knp_menu_render('main', {'currentClass': 'active', 'template': 'AppBundle:menu:knp_menu.html.twig'}) }}
                    </div>
                </div>
            </nav>
        </div>

        {% block header %}{% endblock %}
        <div class="container">

            <div class="main-container">
                <div class="container">
                    {% block body %}{% endblock %}
                    {% block fos_user_content %}{% endblock %}
                </div>
            </div>

        </div>
        </div>


        {% javascripts '@jquery' '@bootstrap_js' %}
        <script type="text/javascript" src="{{ asset_url }}"></script>
        {% endjavascripts %}
        <script src="{{ asset('js/parallax.min.js') }}"></script>
        {% block javascripts %}{% endblock %}
    </body>
</html>
